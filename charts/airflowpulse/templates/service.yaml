apiVersion: v1
kind: Service
metadata:
  name: {{ include "airflowpulse.fullname" . }}
  labels:
    {{- include "airflowpulse.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "airflowpulse.selectorLabels" . | nindent 4 }}
